{{ui/icon-button
    class='record-button'
    loading=saving
    disabled=saving
    mouseDown=(action 'startRecording')
    mouseUp=(action 'stopRecording')
    leftIcon="circle"}}

{{#if hasAudio}}
  {{ui/icon-button
      click=(action "play")
      leftIcon="play"}}
{{/if}}
